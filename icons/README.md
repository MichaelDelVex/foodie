# Foodie icons

Save the original logo as `icons/source-logo.png`, then run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/generate-icons.ps1
```

The generated files are used for browser favicons, iOS home-screen icons, and installable web app icons.
